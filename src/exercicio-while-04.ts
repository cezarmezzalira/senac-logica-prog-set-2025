// 4 - Faça um programa que peça ao usuário para 
// digitar uma palavra e verifique se essa palavra 
// é um palíndromo (ou seja, se pode ser lida da mesma 
// forma da esquerda para a direita e da direita para a esquerda).

import Scanner from "@codeea/scanner"

async function main() {
    const scanner = new Scanner();
    // Insira a solução aqui
    scanner.close();
}

main();